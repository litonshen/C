#include <stdio.h>

int main()
{
    int N;
    scanf("%d", &N); // user input//
    if (N == 0)      // Start the condition//
    {
        printf("Zero\n"); // print Output//
    }
    else
    {
        printf("Non Zero");
    }

    return 0;
}
